@using ECommerceWebsiteMVC_Admin.Models
@model CampaignInfo
@{
    ViewBag.Title = "Sửa Campaign";
    Layout = "~/Views/Shared/_QuanLyNguoiDungHeThongLayout.cshtml";
    var danhMucs = ViewBag.DanhMucs as List<DanhMuc> ?? new List<DanhMuc>();
    var sanPhams = ViewBag.SanPhams as List<SanPham> ?? new List<SanPham>();
    var selectedSanPhams = ViewBag.SelectedSanPhams as List<int> ?? new List<int>();
    var selectedDanhMucs = ViewBag.SelectedDanhMucs as List<int> ?? new List<int>();
}

<!-- PageHeading -->
<div class="flex flex-wrap items-center justify-between gap-3 mb-6">
    <div class="flex flex-col gap-1">
        <h1 class="text-[#181210] dark:text-white text-3xl font-bold leading-tight tracking-tight">Sửa Campaign</h1>
        <p class="text-gray-500 dark:text-gray-400 text-base font-normal leading-normal">Chỉnh sửa chương trình khuyến mãi</p>
    </div>
</div>

<div class="bg-white dark:bg-[#2c1d17] rounded-lg shadow-sm p-6 max-w-5xl">

    @if (ViewBag.Error != null)
    {
        <div class="mb-4 p-4 bg-red-100 dark:bg-red-900/20 text-red-700 dark:text-red-300 rounded-lg">
            @ViewBag.Error
        </div>
    }

    <form method="post" class="space-y-6">
        <input type="hidden" name="MaCampaign" value="@Model.MaCampaign" />

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">

            <!-- Tên Campaign -->
            <div class="md:col-span-2">
                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                    Tên Campaign <span class="text-red-500">*</span>
                </label>
                <input name="TenCampaign" 
                       value="@Model.TenCampaign"
                       required
                       class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md bg-transparent
                              focus:ring-primary focus:border-primary text-sm" />
            </div>

            <!-- Mô tả -->
            <div class="md:col-span-2">
                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                    Mô tả
                </label>
                <textarea name="MoTa" 
                          rows="3"
                          class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md bg-transparent
                                 focus:ring-primary focus:border-primary text-sm">@Model.MoTa</textarea>
            </div>

            <!-- Loại giảm giá -->
            <div>
                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                    Loại giảm giá <span class="text-red-500">*</span>
                </label>
                <select name="LoaiGiamGia" 
                        required
                        class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md bg-transparent
                               focus:ring-primary focus:border-primary text-sm">
                    <option value="PhanTram" @(Model.LoaiGiamGia == "PhanTram" ? "selected" : "")>Phần trăm (%)</option>
                    <option value="SoTien" @(Model.LoaiGiamGia == "SoTien" ? "selected" : "")>Số tiền cố định (đ)</option>
                </select>
            </div>

            <!-- Giá trị giảm -->
            <div>
                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                    Giá trị giảm <span class="text-red-500">*</span>
                </label>
                <input type="number" 
                       name="GiaTriGiam" 
                       value="@Model.GiaTriGiam.ToString(System.Globalization.CultureInfo.InvariantCulture)"
                       min="0"
                       step="0.01"
                       required
                       class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md bg-transparent
                              focus:ring-primary focus:border-primary text-sm" />
            </div>

            <!-- Ngày bắt đầu -->
            <div>
                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                    Ngày bắt đầu
                </label>
                <input type="datetime-local" 
                       name="NgayBD" 
                       value="@(Model.NgayBD.HasValue ? Model.NgayBD.Value.ToString("yyyy-MM-ddTHH:mm") : "")"
                       class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md bg-transparent
                              focus:ring-primary focus:border-primary text-sm" />
            </div>

            <!-- Ngày kết thúc -->
            <div>
                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                    Ngày kết thúc
                </label>
                <input type="datetime-local" 
                       name="NgayKT" 
                       value="@(Model.NgayKT.HasValue ? Model.NgayKT.Value.ToString("yyyy-MM-ddTHH:mm") : "")"
                       class="w-full px-4 py-2 border border-gray-300 dark:border-gray-600 rounded-md bg-transparent
                              focus:ring-primary focus:border-primary text-sm" />
            </div>

            <!-- Trạng thái -->
            <div class="md:col-span-2">
                <label class="flex items-center gap-2 cursor-pointer">
                    <input type="checkbox" 
                           name="TrangThai" 
                           value="true"
                           @(Model.TrangThai ? "checked" : "")
                           class="rounded border-gray-300 dark:border-gray-600" />
                    <span class="text-sm font-medium text-gray-700 dark:text-gray-300">Kích hoạt campaign</span>
                </label>
            </div>

        </div>

        <!-- Chọn sản phẩm và danh mục -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-6 pt-6 border-t dark:border-gray-700">
            
            <!-- Chọn sản phẩm -->
            <div>
                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                    Chọn sản phẩm
                </label>
                <div class="border border-gray-300 dark:border-gray-600 rounded-md p-4 max-h-64 overflow-y-auto">
                    @if (sanPhams.Any())
                    {
                        foreach (var sp in sanPhams)
                        {
                            <label class="flex items-center gap-2 mb-2 cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-800 p-2 rounded">
                                <input type="checkbox" 
                                       name="selectedSanPhams" 
                                       value="@sp.MaSanPham"
                                       @(selectedSanPhams.Contains(sp.MaSanPham) ? "checked" : "")
                                       class="rounded border-gray-300 dark:border-gray-600" />
                                <span class="text-sm">@sp.TenSanPham</span>
                            </label>
                        }
                    }
                    else
                    {
                        <p class="text-sm text-gray-500 dark:text-gray-400">Chưa có sản phẩm nào</p>
                    }
                </div>
            </div>

            <!-- Chọn danh mục -->
            <div>
                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                    Chọn danh mục
                </label>
                <div class="border border-gray-300 dark:border-gray-600 rounded-md p-4 max-h-64 overflow-y-auto">
                    @if (danhMucs.Any())
                    {
                        foreach (var dm in danhMucs)
                        {
                            <label class="flex items-center gap-2 mb-2 cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-800 p-2 rounded">
                                <input type="checkbox" 
                                       name="selectedDanhMucs" 
                                       value="@dm.MaDanhMuc"
                                       @(selectedDanhMucs.Contains(dm.MaDanhMuc) ? "checked" : "")
                                       class="rounded border-gray-300 dark:border-gray-600" />
                                <span class="text-sm">@dm.TenDanhMuc</span>
                            </label>
                        }
                    }
                    else
                    {
                        <p class="text-sm text-gray-500 dark:text-gray-400">Chưa có danh mục nào</p>
                    }
                </div>
            </div>

        </div>

        <!-- Buttons -->
        <div class="flex justify-end gap-3 pt-4 border-t dark:border-gray-700">
            <a href="@Url.Action("QuanLyCampaign", "QuanLyNguoiDungHeThong")" 
               class="px-6 py-2 text-sm font-medium text-gray-700 dark:text-gray-300 bg-gray-200 dark:bg-gray-700 rounded-md hover:bg-gray-300 dark:hover:bg-gray-600">
                Hủy
            </a>
            <button type="submit" 
                    class="px-6 py-2 text-sm font-medium text-white bg-primary rounded-md hover:bg-orange-600">
                Lưu
            </button>
        </div>

    </form>

</div>

